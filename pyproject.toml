[project]
name = "covid-cricket"
version = "0.1.0a0"
requires-python = ">=3.12"
dependencies = ["pandas>=2.2", "pyarrow>=21", "pyfixest>=0.60", "scipy>=1.14"]

[tool.black]
line-length = 88

[tool.isort]
profile = "black"
